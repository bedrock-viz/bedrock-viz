/*
  Minecraft Bedrock Edition World File Visualization & Reporting Tool
  (c) SCP-R 2019

  GPL'ed code - see LICENSE
*/
#pragma once

namespace mcpe_viz {
    const char * const version = R"(${PROJECT_VERSION})";
    const char * const version_full = R"(bedrock-viz v${PROJECT_VERSION})";

    const char * const cmake_install_prefix = R"(${CMAKE_INSTALL_PREFIX})";

    const int kMaxBiomeCount = 256;
    const int kMaxBlockCount = 2048; // FIXME
    const int kMaxItemCount = 2048;  // FIXME
    const int kMaxEntityCount = 256;
    const int kMaxEnchantmentCount = 64;
}
